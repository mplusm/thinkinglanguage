# Plan 08-01: Sampling Handler

## Goal
Implement MCP sampling protocol so servers can request LLM completions through TL.

## Tasks
1. Refactor TlClientHandler to hold SamplingCallback
2. Implement create_message on ClientHandler trait
3. Wire sampling callback in VM and interpreter McpConnect
4. Tests
