# Plan 08-02: Timeout & Error Recovery

## Goal
Add connection/tool call timeouts and graceful error handling.

## Tasks
1. Connection timeout (30s default)
2. Tool call timeout (60s default)
3. Graceful server crash handling
4. Tests
