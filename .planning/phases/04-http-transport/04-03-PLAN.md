# Plan 04-03: End-to-end HTTP Transport Tests

## Goal
End-to-end TL script test and security verification for HTTP transport.

## Tasks

### Task 1: TL script test for HTTP transport
File: `tests/mcp_http.tl`

Test connecting to an HTTP MCP server from TL:
```
let client = mcp_connect("http://127.0.0.1:PORT/mcp")
let tools = mcp_list_tools(client)
// verify tools, call_tool, ping, disconnect
```

### Task 2: Security policy enforcement
- HTTP connections blocked in sandbox mode
- Unit tests for security policy checks

### Task 3: Error handling tests
- Invalid URL → McpError::ConnectionFailed
- Connection refused → appropriate error

## Verification
- `cargo test --workspace`
- TL script passes in both backends

## Dependencies
04-01, 04-02

## Wave
2
